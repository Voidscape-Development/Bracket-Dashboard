/**
 * A small fake bracket used by the theme editor's preview, so styling works
 * before any tournament has been imported.
 */
import { type TournamentSet } from '@bracket/shared';
export declare const previewSets: TournamentSet[];
//# sourceMappingURL=previewData.d.ts.map